package com.kn.tip.problem.ctci;

import com.kn.tip.datastructure.TreeNode;

public class PathWithSum {
  // Path does not need to start at root or end at leave.
  public static int countPath(TreeNode<Integer> root, int value) {
    return 0;
  }
}
